Assignment: create a dictionary of dictionarys. The main dictionary key will be the name. The value will be another dictionary. The other dictionary will have the following keys: "firstname" "lastname" "age". Story the keys in a list.  Then print the screen the age of the first one. Then, store the keys in a list. Use the list to print the entire dictionary to the screen everything. Format the output to make it easy to read. Do not get input from the console anywhere in this exersize. This excersize is meant to give you practice working with dictionaries.